<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>544646</title>
    <script src="@Href("~/")Scripts/jquery.js"></script>
    <script src="@Href("~/")Scripts/common.js"></script>
    <style type="text/css">
        html,body{
            width: 100%;
            height: 100%;
            margin: 0;
            padding: 0;
        }
        li{list-style: none;}
        ul{
            margin: 0;
            padding: 0;
        }
        .page{
            width: 100%;
            height: 100%;
            text-align: center;
            background-image: url(@Href("~/")Content/image/middleR.jpg);
            background-size: 100%;
            position: relative;
        }
        .head{
            color: #fff;
            font-size: 40px;
            margin-top: 50px;
            display: inline-block;
        }
        .show{
            background: #fff;
            width: 300px;
            height: 150px;
            margin: 0 auto;
            margin-top: 70px;
            border-radius:10px;
        }
        .liUname input,.liUpwd input{
            border:0.5px solid #ccc;
            margin-left: 5px;
            height: 20px;
            outline: none;
        }
        input{
            border:0.5px solid #ccc;
            outline: none;
            cursor: pointer;
        }
        .show li{
            padding-top: 20px;
        }
        .liUbtn input:nth-child(2){
            margin-left: 70px;
        }
        .foot{
            bottom: 40px;
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
        }
    </style>

</head>
<body>
    <div class="page">
        <div class="head">
            <span id="labCompany">东莞惠森木业有限公司</span>
            <br />
            信息管理系统
        </div>
        <div class="show">

            <form class="middle">
                <ul>
                    <li class="liUname">
                        <label for="textUsername">用户</label>
                        <input name="txtUsername" type="text" id="txtUsername" class="inputText" autofocus="autofocus" />
                    </li>
                    <li class="liUpwd">
                        <label for="txtPassword">密码</label>

                        <input name="txtPassword" type="password" id="txtPassword" class="inputText" />
                    </li>
                    <li class="liUbtn">
                        <input type="button" name="" value="登录" id="btnLogin">
                        <input type="reset" name="" value="取消"></li>
                </ul>
            </form>

        </div>
        <div class="foot">Copyright &copy; 2013-2014 WoodenoxTools All Rights Reserved</div>
    </div>

</body>
    <script type="text/javascript">
    $(function () {
        $("#btnLogin").click(function () {
            $.ajax({
                method: 'post',
                url: getRootPath() + "/user/login",
                dataType: 'json',
                // 用户名和密码
                data: {
                    Username: $("#txtUsername").val(),
                    Password: $("#txtPassword").val()
                },
                success: function (data) {
                    if (data) {
                        window.location.href = getRootPath() + "/user/notice";
                    } else {
                        alert("用户名或密码错误");
                    }
                }
            });
        })
    })

</script>
</html>